Rebased on main after #239 (MRGFY-8978, the terminal block in plain words) merged. Adopted its sentence table for the four NotAppliedReason values it already carried; on a declined answer the sentence is keyed by the run's own not_applied_reason, since Mergify's reason now survives verbatim and would read as a reduction. Two of #239's tests pinned the intersection and the reason overwrite this change removes; rewritten to the all-or-nothing rule.
